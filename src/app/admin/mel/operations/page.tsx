import Link from "next/link"; import { ArrowLeft } from "@phosphor-icons/react/dist/ssr"; import { OperationsWorkspace } from "@/components/mel/phase5/OperationsWorkspace"; import { getMelOperationsWorkspace } from "@/lib/actions/mel-operations";
export default async function MelOperationsPage() { const result = await getMelOperationsWorkspace(); if (!result.success || !result.data) return <p className="container mx-auto px-4 py-12 text-sm text-red-700">{result.error}</p>; return <div className="container mx-auto space-y-7 px-4 py-8"><header><Link href="/admin/mel" className="inline-flex items-center gap-1 text-sm font-medium text-brand-blue hover:underline"><ArrowLeft className="size-4" /> MEL configuration</Link><h1 className="mt-3 text-2xl font-bold text-slate-900">Operational readiness and rollout</h1><p className="mt-1 max-w-3xl text-sm text-slate-600">Evidence-backed completion gates, feature controls, import health, and correlation-safe operational events.</p></header><OperationsWorkspace data={result.data} /></div>; }

import Link from "next/link"; import { ArrowLeft } from "@phosphor-icons/react/dist/ssr"; import { ImportWorkspace } from "@/components/mel/phase5/ImportWorkspace"; import { getMelImportWorkspace } from "@/lib/actions/mel-imports";
export default async function MelImportsPage() { const result = await getMelImportWorkspace(); if (!result.success || !result.data) return <p className="container mx-auto px-4 py-12 text-sm text-red-700">{result.error}</p>; return <div className="container mx-auto space-y-7 px-4 py-8"><header><Link href="/admin/mel/operations" className="inline-flex items-center gap-1 text-sm font-medium text-brand-blue hover:underline"><ArrowLeft className="size-4" /> Operations</Link><h1 className="mt-3 text-2xl font-bold text-slate-900">Mobile and historical imports</h1><p className="mt-1 max-w-3xl text-sm text-slate-600">Authenticated, version-mapped ingestion with duplicate protection, quarantine, and explicit review promotion.</p></header><ImportWorkspace data={result.data} /></div>; }

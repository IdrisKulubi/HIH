/** Minimum logged support sessions for pipeline completeness (if bootcamp weeks not used). */
export const CDP_MIN_SUPPORT_SESSIONS = 6;

/** Full bootcamp curriculum weeks for pipeline completeness alternative. */
export const CDP_BOOTCAMP_WEEKS = 13;

/** KR weights must sum to 100% within this tolerance (floating point). */
export const CDP_KR_WEIGHT_SUM_TOLERANCE = 0.01;
